import styled from 'styled-components'
import { BaseSubTitle } from '../../styles/Base'

export const Progress = styled(BaseSubTitle)`
  margin-top: 1em;
  color: ${props => props.theme.colors.fuschia}
`
